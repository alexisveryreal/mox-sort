# mox-sort

Project that takes a Moxfield URL and gives you a sorted output in the terminal. Similar to that that shows in Moxfield.

So Commanders -> Creatures -> etc.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts <MOXFIEELD_URL>
```
